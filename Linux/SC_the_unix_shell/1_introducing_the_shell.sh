#! /bin/sh

# ls(listing): lists the content of the current directory
ls

# when trying to run a command that doesn't exist(most likely due to mis-typing)
# the shell will show a message with the command you tried folowed by command not
# found.
# ex.
# $ ks
# ks: command not found